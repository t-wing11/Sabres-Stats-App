# Sabres-Stats-App

App that utilizes the nhl api to lookup previous buffalo sabres rosters and view player stats

Back End

Graphql Apollo server, cd into the graphl folder and use node index.js to run it. 
Will use localHost 4000.

App

Android app using jetpack ui, fetches graphql queries from backend server.
Must run on device on same network as desktop
